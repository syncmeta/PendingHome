# 复制成 secrets.yaml 后填真值。secrets.yaml 已被 .gitignore 忽略。
# 键名与 cct-driver/firmware/secrets.yaml.example 完全一致 —— 想共用一份的话,
# 直接软链过去即可:  ln -s ../../cct-driver/firmware/secrets.yaml secrets.yaml
wifi_ssid: "YOUR_SSID"
wifi_password: "YOUR_PASSWORD"
# 生成:openssl rand -base64 32
api_encryption_key: "REPLACE_ME"
ota_password: "REPLACE_ME"
