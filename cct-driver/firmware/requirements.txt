# 刷机 / 编译用的 ESPHome 版本锁。改这里之前先读 docs/bring-up-checklist.md §0.7。
#
# 2026-08-18 锁定在 2026.7.4:当天 PyPI 上的最新正式版(2026.8.0 只有 beta),
# 也是 hardware/pre-order-circuit-review-2.md 复现 🔴-4 时用的那个版本。
# 本仓库 firmware/ 下的 `esphome config` / `esphome compile` 结果都是在它上面取得的。
#
# 需要 Python 3.12 - 3.14(2026.7.4 的 requires_python 是 >=3.12,<3.15)。
#
# 升级步骤:改这里 -> 在干净 venv 里 `esphome config` 和 `esphome compile` 两条都跑通
#          -> 更新 docs/bring-up-checklist.md §0.7 -> 才算换完。
esphome==2026.7.4
