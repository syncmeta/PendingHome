# v2 楼层规划(floorplan)

**状态:待人类过目拍板。本轮只出文档与图,`cct-main.kicad_pcb` / `cct-main.kicad_sch` / `gerber/` 一个字节没动。**

- 配图:[`floorplan-v2.svg`](floorplan-v2.svg) / [`floorplan-v2.png`](floorplan-v2.png)(1:1 mm),生成脚本 [`gen_floorplan_svg.py`](gen_floorplan_svg.py)
- 坐标系:原点左上角,x 向右,y 向下,单位 mm。**文件方向 = 上墙安装方向(接线端子在下边)**,与现版一致。
- 所有封装尺寸取自 `cct-main.kicad_pcb` 的 F.CrtYd(courtyard)实测值;凡是估算的都写了"估算"。

## 为什么要有这份文件

现版板子过了 DRC 也过了嘉立创 DFM,但它**没有楼层规划**:摆位来自 `gen_pcb.py`(386 行),
布线来自自动布线器输出(`cct.dsn`/`cct.ses`)加 20 个 `gen_route_repair*.py`(约 3000 行)
一轮轮打补丁。人类看着实物说的四条 —— 布局走线乱、入电电路在最中间、丝印标号对不上、
元件摆放没对应电流和逻辑走向 —— **本质上都是"先布线后规划"的必然结果**。

这份文件补的就是那份缺失的设计:**先定电流往哪走,再定元件摆哪儿**。

---

## A1. 设计原则

后面每一个决定都要能追溯到这六条中的某一条。原则按优先级排,冲突时上条压下条。

### 原则 1 —— 功率电流单向,不折返

15A 主干从进线到六路分配点,**每一段只能沿一个方向走**:先向上(穿过保护链)、
再向左(分配脊椎)、再向下(六列功率级)。不允许出现"走到某处再掉头回来"的段。

> 现版违反:`V24_PROT` 从 Q1/Q2(y=115.7)沿右板边**上行**到 y=52 去喂 C1–C5,
> 而真正配电的 `V24_BUS` 从 RS1(y=98.7)**下行**回到 y≈130 的底边横铜,再左行 84mm。
> 中间那 33mm 是原路折返。

**毫安级辅助馈线不受此约束**(例如 `V24_LOGIC` 从 D0 区上行 40mm 到 buck),
但必须用细线(≤1mm)、走板边、并在本文件里点名,不得混进脊椎铜面。

### 原则 2 —— 强弱电分区,交界处只允许定义好的穿越点

24V/15A 的铜面、3A 的支路铜面、5V 栅极信号、3.3V 逻辑各自有连续的地盘。
强弱电只在**一条水平界线**(分配脊椎带 y 73–85)上相遇,穿越点数量、位置、宽度全部写死:
**12 根栅极信号,分 6 条车道(每列 1 条、宽 6mm),只走底层,顶层脊椎铜不断。**

不允许出现"某根信号临时找个缝钻过去"。

### 原则 3 —— 驱动器压在它所驱动的负载质心上

栅极驱动器、栅阻、下拉、去耦,统统按"谁驱动谁"就近摆,不按"IC 排排坐"摆。

> 现版违反:U7 在 x=54,却驱动 x=24 与 x=10 的 CH5/CH6;U6 在 x=76,驱动 x=80/66/52/38 的
> CH1–CH4。栅极信号因此要横跨 44–57mm 并**穿插进别的通道列**。

v2:U6 压在 CH1–CH4 的 x 质心(**x=68**),U7 压在 CH5–CH6 的 x 质心(**x=20**)。
**脊椎以下,每一列只走本列自己的网络** —— 这是一条可以逐根走线检查的硬指标。

### 原则 4 —— 丝印空间是硬预算,不是事后补

位号所需的空间在**摆位阶段**就分配掉,不是布完再找地方塞。
具体预算见 §A6:**同类元件行与行之间净距 ≥1.8mm(设计值取 2.2mm)**。
达不到这个数的行,要么拉开,要么这一行的元件就注定没有位号 —— 二选一,当场决定,不留到最后。

> 现版违反:28 个元件没有位号,不是不想标,是**物理上排不下**
> (LED2–LED13 与它们的限流电阻中心距只有 2.1mm;R59–R61 所在的两排 0603 行净距 1.61mm,
> 而位号文字本身就 1.0mm 高)。

### 原则 5 —— 每一条约束都要能被一个数检查

"尽量靠近""合适的距离"这种话在本文件里一律换成数字。
`gen_silk_refdes_fix.py` 的判据(§A6)、脊椎压降(§A4)、镊子净空(§A5 第 6 条)
都是这样的数;新布局做完后逐条量,不达标就是没做完。

### 原则 6 —— 保住现版已经做对的东西

现版**六路功率级本身是规整的**:14mm 等距,六路的 MOSFET / 电解 / 保险丝 / 端子各自严格同一 y,
一个不差。这部分是这块板做得最好的地方,v2 **完整保留这个"六列等距同行"的骨架**,
只把列距从 14mm 放宽到 16mm、把列内的行序按电流方向重排。

---

## A2. 电流走向 与 逻辑走向

### A2.1 24V 功率电流(进线 → 灯带 → 地)

```
                         ┌───────────────── 板子右下角 ─────────────────┐
                         │                                             │
  24V+ ──► J1(116,137) ──┤                                             │
                         ▼  ①向上,一条直线,不折返                     │
                    F1 主保险丝 15A ATO            (y=122)              │
                         ▼                                             │
                    Q1 ‖ Q2  防反接 P-MOS         (y=111.5)            │
                         ▼        ├─► DZ1 栅极钳位 / R1 / Q3 总断路      │
              V24_PROT 节点 ──────┼─► C1 C2 C3  100µF   (y=100)        │
                         │        ├─► C4 C5      100µF   (y=90.5)      │
                         │        ├─► D1 SMBJ26A TVS                    │
                         │        └─► PTC1 ──► V24_LOGIC ──► buck(细线上行)
                         ▼
                    RS1 2mΩ 采样  (y=82)  ◄── U1 INA237 开尔文两点
                         │
                    V24_BUS 分配点 (x≈102, y≈80)
                         │
   ②向左 ────────────────┴─────────────────────────────────────────────►
   ╔═══════════════════════════════════════════════════════════════════╗
   ║  B0  24V 分配脊椎   y 73–85   x 8.5–100   双面 12mm 铜 + 缝合过孔    ║
   ╚═╤═════════╤═════════╤═════════╤═════════╤═════════╤═══════════════╝
     │ CH6     │ CH5     │ CH4     │ CH3     │ CH2     │ CH1      ③向下
     ▼ x=12    ▼ x=28    ▼ x=44    ▼ x=60    ▼ x=76    ▼ x=92
   ┌───────┐ 每一列都是同一个顺序,六列一模一样:
   │ Fn    │  支路保险丝 4A T          y=89.6
   │ ↓     │
   │ Cn +  │  100µF 电解 + 2 颗指示灯 + 2 颗 40.2k 限流       y=104.4
   │ ↓     │
   │ D TVS │  SMBJ26A ×2(漏极→GND,只在异常瞬态导通)      y=112.6
   │ D 续流│  SS36B ×2(阳极→漏极,阴极→CHn_VOUT)          y=118.5
   │ R 栅  │  100Ω 栅阻 ×2 + 10k 下拉 ×2 + 100nF 去耦        y=123.0
   │ Q MOS │  20N06 ×2  并排,漏极正对端子                    y=128.7
   │ Jn    │  输出端子 V+ / CW / WW                          y=137.5
   └───┬───┘
       │  ④ 灯带在板外
       ▼
   [ 灯带 CW / WW ]  ── 电流从 V+ 出去,从 CW/WW 回来 ──►  MOS 漏极
                                                              │
                                                              ▼ MOS 源极
   ⑤ GND 回流:6 路源极 ──► 底部带(y ≥ 133)一路向右 ──► J1 pin2 (24V−)
      功率地全程落在 y ≥ 133 的底部带内,不进入 y < 85 的逻辑区。
```

**四段,四个方向,不重复走任何一段:**

| 段 | 方向 | 长度(估算,焊盘到焊盘) | 载流 |
|---|---|---|---|
| ① J1 → RS1(穿 F1 / Q1Q2 / 体电容) | 向上 | 55mm | 15A |
| ② RS1 → 分配脊椎 → CH6 保险丝上游 | 向左 | 93mm | 15A 递减到 3A |
| ③ 脊椎 → 保险丝 → 端子 | 向下 | 每列 10.6 + 47.9 = 58.5mm | 3A |
| ⑤ MOS 源极 → J1 pin2 | 向右 | ≈110mm | 汇总 12A |

现版同一条路径是 **上行 38.3 → 左行 7 → 掉头下行 33.3 → 左行 81 → 上行 3.7 → 下行 10.7**,
到最远那路(CH6)合计约 **174mm**,中间 33.3mm 是原路折返。
v2 到 CH6 合计约 **206mm**,**没有任何一段回头**。

> **要诚实说清楚:v2 的 24V 路径反而长了约 32mm。** 原因是把分配脊椎从底边(y≈130)
> 搬到了 y=79 —— 换来的是"每一列从上到下一个顺序"这件事(原则 1 + 原则 6)。
> **多出来的长度用线宽补回来,而且补过头了:**
>
> | | 现版 | v2 |
> |---|---|---|
> | `CHn_VOUT`(保险丝→端子) | 2.0mm × 10.7mm = **2.6 mΩ** | **3.0mm** × 47.9mm = **7.9 mΩ** |
> | `CHn_CW_D`(端子→漏极) | 1.2mm × 24.3mm = **10.0 mΩ** | 2.0mm × 13mm = **3.2 mΩ** |
> | `CHn_WW_D` | 1.2mm × 41.7mm = **17.1 mΩ** | 2.0mm × 13mm = **3.2 mΩ** |
> | **单路总串阻** | CW **12.6 mΩ** / WW **19.7 mΩ** | **11.0 mΩ,两路一样** |
> | **3A 压降** | 38 / 59 mV | **33 / 33 mV** |
>
> 电阻率模型已用 `layout-guide.md` 里那三个实测值校准过(2.0mm/10.7mm→2.6mΩ、
> 1.2mm/24.3mm→10.0mΩ 都能对上)。**两路等长这件事对 CW/WW 的色温一致性是加分项**,
> 现版 CW 与 WW 的铜阻差了 7.1mΩ。

### A2.2 逻辑与信号走向

```
   上板边(插拔方向朝外)
   ┌───────────────────────────────────────────────────────────────┐
   │ [U4 天线]  J2 Type-C   │  J11 干接点 5P   J10 UART 4P   J9 I2C │
   └────┬──────────┬────────┴────────┬──────────┬──────────┬───────┘
        │          │                 │          │          │
        │      USB_DP/DM         SW_T1..4    UART2_TX/RX  SDA/SCL
        │          ▼                 ▼          │          │
        │     U5 CH340C ──U0TXD/RXD──┐    R54–57 串阻      │
        │     Q4/Q5 自动下载 ──EN/IO0─┤    R58–61 上拉      │
        │                            ▼    C28–31 消抖      │
        └──────────────────────►  U4  ESP32-WROOM-32E  ◄───┴──── R52/R53 上拉
                                    │                    (I2C 也去 U1 INA237)
              12 路 LEDC PWM(3.3V)  │  OE_CTRL(GPIO15)
                                    ▼
             ┌──────────────────────────────────────────────┐
             │ A5 栅极驱动区  y 62–71                        │
             │   U7 @x=20  ← CH5,CH6      U6 @x=68 ← CH1–4  │
             │   R13/R14/R15/Q6:/OE 隔离(硬件失效安全)      │
             └───┬────┬────┬────┬────┬────┬─────────────────┘
                 │    │    │    │    │    │   12 根 CHn_xx_G(5V)
   ══════════════╪════╪════╪════╪════╪════╪══════════════════════
    B0 脊椎 y73–85 每列一条 6mm 底层车道,顶层脊椎铜不断
   ══════════════╪════╪════╪════╪════╪════╪══════════════════════
                 ▼    ▼    ▼    ▼    ▼    ▼   垂直下行,不跨列
              CH6  CH5  CH4  CH3  CH2  CH1
               └─► 100Ω 栅阻 → MOS 栅极;10k 下拉在栅极上(y=123.0)
```

**一句话:逻辑从左上角进,一路向下,最后 12 根平行下到六列 —— 和 24V 同一个方向。**

- I2C:J9(上板边)→ R52/R53 上拉 → U4 GPIO32/33,并**沿右板边下行**到 D0 区的 U1(INA237)。
  这是唯一一条从逻辑区伸进功率区的信号,走右板边,不穿脊椎中段。
- UART2:J10(上板边)→ U4 GPIO26/27,全程在 A3/A1 区内。
- USB:J2(上板边)→ U5 → U4,全程在 A2 区内;`USB_VBUS` 经 D4 进 A4 区的 `V5_SYS`。
- 干接点:J11(上板边)→ R54–57 串阻(**端子侧**)→ R58–61 上拉 + C28–31(**MCU 侧**)→ U4。

---

## A3. 分区表

板框 **130 × 145 mm**(推荐值,论证见 §A4)。

| 区 | 名称 | x 范围 | y 范围 | 面积 | 元件数 | 为什么在这儿 |
|---|---|---|---|---:|---:|---|
| A0 | 天线净空区 | 0–8 | 0–25 | 200 mm² | 0 | U4 天线段所在。**双面禁铜、无元件、无过孔、无螺丝**,靠左板边。现版这块是被 GND 覆铜双面填满的(§A7 第 2 条) |
| A1 | 主控区 | 0–40 | 0–62 | 2480 mm² | 10 | U4 长轴水平、天线朝左出板边;复位/BOOT 键、状态灯就近 |
| A2 | USB / 串口区 | 40–66 | 0–62 | 1612 mm² | 9 | J2 Type-C 在上板边插拔朝外;CH340C 与自动下载电路紧跟其后,USB 差分对最短 |
| A3 | 传感器 / 干接点接口区 | 66–101 | 0–26 | 910 mm² | 17 | 三个接口全在上板边、物理防呆;串阻在端子侧、上拉在 MCU 侧,顺序不可颠倒 |
| A4 | 低压电源区 (buck + OR + LDO) | 66–130 | 26–62 | 2304 mm² | 26 | 与 U4 天线拉开 ≥60mm;SW 节点与 L1 远离逻辑与天线;`V24_LOGIC` 从 D0 区沿右板边细线上行到此 |
| A5 | 栅极驱动区 | 4–101 | 62–71 | 873 mm² | 9 | 紧压在脊椎上沿,U6/U7 分别落在自己那几路的 x 质心上 |
| B0 | 24V 分配脊椎(纯铜箔) | 8.5–100 | 73–85 | 1098 mm² | 1 | 强弱电唯一的交界带。双面 12mm 铜、每 3mm 一颗 0.5mm 缝合过孔;底层每列让出 6mm 信号车道 |
| C1 | 功率通道列 CH1 | 84–100 | 85–145 | 960 mm² | 19 | 离分配点最近的一路放 CH1,与丝印编号方向一致(右→左 = CH1→CH6) |
| C2 | 功率通道列 CH2 | 68–84 | 85–145 | 960 mm² | 18 | 同上 |
| C3 | 功率通道列 CH3 | 52–68 | 85–145 | 960 mm² | 18 | 同上 |
| C4 | 功率通道列 CH4 | 36–52 | 85–145 | 960 mm² | 18 | 同上 |
| C5 | 功率通道列 CH5 | 20–36 | 85–145 | 960 mm² | 18 | 同上 |
| C6 | 功率通道列 CH6 | 4–20 | 85–145 | 960 mm² | 18 | 同上 |
| D0 | 入电保护区 | 101–130 | 62–145 | 2407 mm² | 20 | J1 在右下角(24V 从上方电源沿侧缘下行进来);整条保护链自下而上串成一条直线 |
| MH | 安装孔 | — | — | — | 4 | H4(4,30) H3(126,30) H2(4,79) H1(126,79) |

六列功率级的**列心 x**:CH1=92 CH2=76 CH3=60 CH4=44 CH5=28 CH6=12,**列距 16mm**。

### A3.1 功率通道列的行结构(六列完全相同)

行高取该行最高元件的 courtyard 高度,行间净距即丝印预算(§A6)。

| 行 | 内容 | 中心 y | 行高 | 到下一行的净距 |
|---|---|---:|---:|---:|
| 1 | 支路保险丝 Fn(Nano2 座,4A 慢断) | 89.60 | 5.12 | **8.00**(镊子净空) |
| 2 | 电解 100µF + 指示灯 ×2 + 40.2k ×2 | 104.36 | 8.40 | 2.20 |
| 3 | 输出 TVS SMBJ26A ×2 | 112.60 | 3.69 | 2.20 |
| 4 | 续流二极管 SS36B ×2 | 118.50 | 3.69 | 2.20 |
| 5 | 100Ω 栅阻 ×2 + 10k 下拉 ×2 + **100nF 去耦** | 122.99 | 0.89 | 2.20 |
| 6 | 功率 MOS 20N06 ×2(**并排**) | 128.70 | 6.14 | 2.20 |
| 7 | 输出端子 Jn | 137.50 | 7.09 | 3.96(到板边) |

**行 3 与行 4 的顺序是有意的:续流二极管排在更靠近 MOS 的那一行。** 两者电气上都挂在
漏极节点上,但续流管在 19.5kHz 下**每个周期都换流**,而 TVS 只在异常瞬态时导通。
把 `D5/D6` 放在 y=118.5、`C17` 放在 y=123.0、`Q7/Q8` 放在 y=128.7,快速换流环路
`C17 → D5 → Q7 → GND` 被压在 **约 10mm × 8mm = 0.8 cm²** 之内(目标 ≤1.5 cm²,
与 `layout-guide.md` 第 2 条给 buck 开关环路的量级一致)。
现版这个环路是 C17(y=71.8)→ D5(y=121.2)→ Q7(y=107.8),竖直跨度 **49mm**。

- **MOSFET 从上下两排改成并排**:两只 TO-252 各 6.69mm 宽,16mm 列距下并排后互相净距
  1.30mm、与邻列净距 1.30mm。好处是两路(CW/WW)对称,且漏极正对端子。
- **漏极走线从 24.3 / 41.7mm 缩到约 13mm**:现版 `CHn_CW_D` 24.3mm、`CHn_WW_D` 41.7mm
  (`layout-guide.md` 实测),3A 下压降 30 / 51mV,两路还不对称。v2 端子焊盘 y=137.5、
  MOS 漏极片中心 y≈128.7,加横向偏移约 13mm;沿用 1.2mm 线宽即 5.4mΩ / 16mV,
  空间允许时加宽到 2.0mm 则 3.2mΩ / 9.7mV。**两路一样长。**
- **栅阻和下拉都放在栅极端**(现版栅阻在驱动侧、栅极侧只有下拉):
  栅极是高阻节点,从驱动区下来那段约 58mm 的走线要贴着 24V 脊椎和漏极铜走。
  把 100Ω 放在最靠近栅极的位置,使栅极在整个工作频段都是低阻,抗 dV/dt 误导通;
  同时把走线电感(58mm ≈ 58nH 估算)与 Ciss 的谐振阻尼掉 —— 临界阻尼需
  `R > 2·√(L/Ciss) ≈ 2·√(58nH / 0.6nF) ≈ 20Ω`,**100Ω 是过阻尼**。
  这只是**摆位**改动,原理图网络顺序(`CHn_xx_G → R栅阻 → CHn_xx_GR → 栅极 / 10k → GND`)一个字不改。
- **每只 20N06 的漏极散热铜面目标 ≥80 mm²**(顶层)+ ≥4 颗 0.5mm 过孔通到底层同网络铜。
  依据:设计文档 §6.1 按高温 **0.4W/只**预留散热铜面。列内 y 116–134 的 18mm × 每路 8mm
  可用面约 100mm²,16mm 列距下有余量;14mm 列距下约 85mm²,刚够(见 §A4 降级方案)。

### A3.2 入电保护区 D0 的行结构(电流自下而上)

| 行 | 内容 | 中心 y | 行高 | 说明 |
|---|---|---:|---:|---|
| 1 | RS1 2mΩ + U1 INA237 + C6 | 82.0 | 3.29 | 分配点。U1 紧贴 RS1,开尔文两线并行等长 |
| 2 | C4 C5 电解 + D1 TVS | 90.5 | 8.40 | `V24_PROT` |
| 3 | C1 C2 C3 电解 | 100.0 | 8.40 | `V24_PROT` 体电容主力 |
| 4 | Q1 ‖ Q2 防反接 P-MOS + DZ1 / R1 / Q3 / R2 / R3 | 111.5 | 6.19 | 两只并排,栅极件贴在右侧空当 |
| 5 | F1 主保险丝座(双联 ATO 15A) | 122.0 | 6.81 | 19.9mm 宽,横放 |
| 6 | J1 24V 输入端子(KF7.62-2P) | 137.0 | 12.09 | 下板边,距板边 1.95mm |

- **体电容到 J1 的距离:37mm(现版 83–101mm)。** 这是"J1 必须在下板边 + F1 与 Q1/Q2
  必须串在中间"两个前提下的物理下限:J1 上缘 y=130.96,C1 下缘 y=104.2,中间 26.8mm 全被
  F1 与两只 P-MOS 占满。想再短只能把 F1 从直插双联 ATO 座换成贴片保险丝(省约 15mm),
  那是另一个决策,本轮不动。
  **真正补在 J1 跟前的是 §C1 的 `V24_IN` RC 缓冲(离 J1 焊盘 ≤8mm)。**
- **F1 与 Q1/Q2 的净距 10.8mm**(现版 3.3mm)。设计文档 §7 要求输入防反 P-MOS
  "另设独立散热铜面并远离保险丝座";P-MOS 的散热铜面(`V24_PROT`)向**上**铺进体电容区,
  热量走向背离 F1。
- 一旦 `V24_PROT` 铜面往上铺到 y≈86,它与脊椎带(y 73–85)只隔 1mm —— **两者电位不同
  (中间隔着 RS1),必须留 ≥1.5mm 间隙,并且这个间隙就是开尔文采样线穿过的地方。**

### A3.3 元件 → 区 完整清点(205 个,一个不漏)

下表由 `gen_floorplan_svg.py` 自动生成:位号全集读自 `cpl-jlc.csv`(195 个贴装件)
再补 10 个非贴装件(H1–H4、TP1–TP6),脚本核对"未归区 / 多余 / 重复"三项必须全为空
才肯写图。跑 `python3 gen_floorplan_svg.py --census` 可复现。

| 区 | 名称 | 元件数 | 位号 |
|---|---|---:|---|
| A1 | 主控区 | 10 | C10 C11 C12 LED1 R4 R5 R8 SW1 SW2 U4 |
| A2 | USB / 串口区 | 9 | C13 J2 Q4 Q5 R9 R10 R11 R12 U5 |
| A3 | 传感器 / 干接点接口区 | 17 | C28 C29 C30 C31 J9 J10 J11 R52 R53 R54 R55 R56 R57 R58 R59 R60 R61 |
| A4 | 低压电源区 (buck + OR + LDO) | 26 | C32 C33 C34 C35 C36 C37 C38 C39 C40 C41 C42 C43 D2 D3 D4 L1 R62 R63 R64 R65 R66 R67 TP3 TP4 U2 U3 |
| A5 | 栅极驱动区 | 9 | C14 C15 Q6 R13 R14 R15 TP5 U6 U7 |
| B0 | 24V 分配脊椎(纯铜箔) | 1 | TP1 |
| C1 | 功率通道列 CH1 | 19 | C16 C17 D5 D6 D7 D8 F2 J3 LED2 LED3 Q7 Q8 R16 R17 R18 R19 R20 R21 TP6 |
| C2 | 功率通道列 CH2 | 18 | C18 C19 D9 D10 D11 D12 F3 J4 LED4 LED5 Q9 Q10 R22 R23 R24 R25 R26 R27 |
| C3 | 功率通道列 CH3 | 18 | C20 C21 D13 D14 D15 D16 F4 J5 LED6 LED7 Q11 Q12 R28 R29 R30 R31 R32 R33 |
| C4 | 功率通道列 CH4 | 18 | C22 C23 D17 D18 D19 D20 F5 J6 LED8 LED9 Q13 Q14 R34 R35 R36 R37 R38 R39 |
| C5 | 功率通道列 CH5 | 18 | C24 C25 D21 D22 D23 D24 F6 J7 LED10 LED11 Q15 Q16 R40 R41 R42 R43 R44 R45 |
| C6 | 功率通道列 CH6 | 18 | C26 C27 D25 D26 D27 D28 F7 J8 LED12 LED13 Q17 Q18 R46 R47 R48 R49 R50 R51 |
| D0 | 入电保护区 | 20 | C1 C2 C3 C4 C5 C6 D1 DZ1 F1 J1 PTC1 Q1 Q2 Q3 R1 R2 R3 RS1 TP2 U1 |
| MH | 安装孔 | 4 | H1 H2 H3 H4 |
| **合计** | | **205** | 未归区:无 · 多余:无 · 重复:无 |

> **数量核对**:205 = 现版板上元件总数,与现版一致。§C 建议的新增件(`C44`/`R68`
> 进线缓冲、`C45`/`C46` 母线陶瓷、`TP7`/`TP8`、`H5`)**都还没算进这 205**;
> 若全部采纳,总数变 **212**,增量落在 D0 区(+4)、B0 区(+2,C45/C46 沿脊椎)、
> A4 或 A1 区(+1,TP8)与安装孔(+1)。

---

## A4. 板框尺寸:结论与论证

### 结论:**推荐 130 × 145 mm(在现版基础上只向右 +20mm,高度不变)**

高度不动是刻意的 —— 亚克力背板 240 × 380 的三明治区高度、装配顺序、走线方案全部不用重算。

### 为什么 110mm 宽装不下

下板边是这块板最挤的一条线,它同时要放 6 个灯带端子和 1 个 24V 输入端子:

| 项 | 实测宽度 |
|---|---|
| J3–J8 输出端子(KF2EDGV-3.81-3P) | 12.31 mm × 6 |
| J1 输入端子(KF7.62-2P) | 16.53 mm |
| 六列所需跨距(列距 p) | 5p + 12.31 |

按 `5p + 12.31 + 端子间空当 + 16.53 + 两侧板边余量` 反推:

- **110mm** → 列距上限约 **13.7mm**(现版做到 14mm,靠把左板边余量压到 3.85mm 换来)
- **120mm** → 列距 15mm 可行,但 D0 区只剩 28mm 宽,体电容排不成 3+2、要排 3 行
- **130mm** → 列距 16mm,D0 区 29mm 宽,F1(19.9mm)与 J1(16.53mm)都横得下,
  体电容 3+2 两行,还能在 J3 与 J1 之间的 9.6mm 空当里加一颗底边安装孔

### 130mm 换来的四样东西(都可以量)

1. **两只输出 MOSFET 并排** → 通道列的竖直行数从 8 行降到 7 行,漏极走线从 24.3/41.7mm
   降到约 13mm,且两路对称(见 §A3.1)
2. **入电保护区独立成列(x 101–130)** → J1、F1、Q1/Q2、体电容、RS1 串成一条直线,
   不再和 CH1 抢地盘;体电容到 J1 从 83–101mm 缩到 37mm
3. **24 颗指示灯与限流电阻拿回位号**(现版排不下,见 §A6)
4. **行间距普遍从 <1mm 提到 ≥2.2mm**,`gen_silk_refdes_fix.py` 的判据能在**摆位阶段**就满足,
   而不是靠事后重摆 52 个 + 隐藏 4 个

### 成本

面积 159.5 cm² → 188.5 cm²(+18%),仍在 150×150 以内。
按机长已确认的前提:整单 ¥1335.66 里板子面积那部分是零头。**本轮没有重新询价。**

### ⚠️ 130mm 的真实代价:亚克力要重画重切

这是本轮**最需要人类拍板**的一件事,而且它和"免费打样 8/31 截止"直接冲突。

| 件 | 现版 | v2(130 宽) | 影响 |
|---|---|---|---|
| 上夹板 `driver-top.svg` | 120×155(186 cm²) | **140×155(217 cm²)** | 仍在免费打样 ≤500cm² 内;**图要重画** |
| 下夹板 `driver-bottom.svg` | 120×155 | **140×155** | 同上 |
| 背板 `backing-board.svg` | 240×380 | 240×380 **不变** | 驱动板区标记宽 +20mm,不影响外形 |
| 绕线盘 | φ30 ×8 | 不变 | — |

安装孔:四个里**只有 H4 (4,30) 位置不变**,H2 从 (4,65) 挪到 (4,79)、
H1 从 (106,79) 挪到 (126,79)、H3 从 (106,12) 挪到 (126,30)。所以夹板必须重画。

**也就是说:如果现在按机长上一条建议把两块夹板切了,采纳 130mm 之后它们就作废。**
两条路:
- **先拍板尺寸,再切亚克力** —— 稳,但可能赶不上 8/31 的免费窗口(那两块要付费约几十块)
- **先切现版夹板,尺寸维持 110×145** —— 省那几十块,代价是下面这张降级清单

### 若硬要维持 110 × 145:降级方案

架构(单向电流、分区、每列只走本列)**全部保留**,但要放弃这些:

| 放弃项 | 具体退到什么程度 |
|---|---|
| MOSFET 并排 | 列距 13.7–14mm 下两只 TO-252 并排后邻列净距 <0.5mm,只能维持现版的**上下两排**;通道列竖直方向多占约 8mm,脊椎带被迫上移到 y 65–77、逻辑区压缩到 y 0–53 |
| 漏极走线 | 回到 24 / 42mm 量级,3A 压降 30 / 51mV,**两路不对称**(现版就是这样) |
| 24 颗 LED / 限流电阻的位号 | 电解 8.4mm + 100nF + 灯 + 电阻挤在 14mm 列宽里,灯与电阻中心距仍只有约 2.1mm,**位号还是排不下** |
| 体电容离 J1 | D0 区只剩 22–23mm 宽,体电容排 3 行,最远一颗到 J1 约 56mm(130 宽时 37mm) |
| 底边安装孔 H5 | 没有 9.6mm 空当,取消 |
| 逻辑区余量 | 从 130×62 = 8060 mm² 压到 110×53 = 5830 mm²,与现版实际占用基本持平,**没有余量** |

**我的建议:选 130×145。** 亚克力那两块的钱(几十块)买回来的是漏极压降减半、
两路对称、24 个位号、以及一块不用再打 20 个补丁脚本的板子。
但**这是钱和时间的取舍,该人类拍板,不该我拍。**

---

## A5. 硬约束逐条对照

来源:`netlist-spec.md`(§Block A–F 关键约束)与 `layout-guide.md`(§硬性几何约束、走线宽度)。
"现版"一列凡是我实测的都标了数,没测的写"未测"。

### A5.1 `layout-guide.md` 硬性几何约束

| # | 约束原文要点 | 现版实测 | v2 怎么满足 |
|---|---|---|---|
| 1 | ESP32 天线净空:模组两侧 ≥10mm、**正下方全层禁铜**,天线区不放铜柱/螺丝 | ❌ **不满足**。用 `HitTestFilledArea` 逐点验过:GND 覆铜在 **F.Cu 与 B.Cu 双面**都填满了天线正下方 (3,12)/(3,5)/(3,19),板上**没有任何 keepout 规则区** | A0 区 x 0–8 / y 0–25 设 **rule area:双面禁铜 + 禁过孔 + 禁元件**;天线朝左出板边(左侧只有 1.2mm 板材,其余是空气);螺丝最近的 H4 在 (4,30),离净空区下沿 5mm |
| 2 | buck 开关环路 <2cm²;L1 紧接 SW;SW 铜面积最小;R62 紧贴 U2 | 未测 | A4 区内 C32/C33 → U2 VIN → D2 → GND 排成闭合小环,目标 ≤1.5cm²;L1 紧邻 SW 脚;R62 放 U2 的 RT/CLK 脚正对面 ≤3mm |
| 3 | INA237 开尔文采样:0.3mm 细线从 RS1 焊盘内侧单独引出,平行等长,不碰功率铜 | 未测 | D0 区第 1 行,U1 与 RS1 同一行、水平相邻;两根 0.3mm 线在 RS1 与 U1 之间并行,长度差 ≤0.5mm;这两根线穿过 `V24_PROT` 与脊椎之间那条 ≥1.5mm 的间隙 |
| 4 | 功率脊椎 15A:双面 ≥20mm 覆铜 + ≥30 个 0.5mm 过孔 + 顶层开窗上锡;分配点后每列 ≥3mm | 现版脊椎是底层 x 7–91 / y 128–132 的一条约 3.7mm 高的铜带 + 顶/底一段 x 86–91 的竖带 | **改成 12mm × 双面 + 每 3mm 一颗 0.5mm 缝合过孔(91.5mm 长 → 约 30 颗)**。压降与载流见下方计算。**这一条我把 20mm 放宽成了 12mm,理由在下面,请一并过目** |
| 5 | GND 单点连接:功率地与信号地在 RS1 附近汇合,信号区不承载功率回流 | 现版是一整片 GND 覆铜(顶+底,x 0.4–109.5 / y 0.4–144.5),没有几何隔离 | 写成两条可检查的规则:**(a)** 功率回流(6 路 MOS 源极 → J1 pin2)全部落在 y ≥ 133 的底部带内,与逻辑区在几何上不重叠;**(b)** U1 的 GND 脚用**独立细线**接到 RS1 下游焊盘旁的地基准点,不与功率回流共用铜。栅极信号的回流由脊椎底层每列 6mm 车道里的 GND 就近提供,不绕远 |
| 6 | 保险丝座 F2–F7 下方留 ≥8mm 镊子空间 | ❌ **不满足**。F2 courtyard y 123.77–128.89;上方 D5 到 123.05(净距 **0.72mm**),下方指示灯到 129.88(净距 **0.99mm**)。括号里"端子与保险丝座之间不放高元件"这层意思是满足的(那些都是 ≤1.5mm 高的贴片) | 保险丝行(y 87.04–92.16)**下方 8.00mm 内无任何元件**(下一行电解顶边 y=100.16);上方 2.04mm 是脊椎铜面,也没有元件。**按字面满足** |
| 7 | 四角 M3 安装孔,中心距板边 5mm;左上那颗避开天线净空 | 现版 H1(106,79) H2(4,65) H3(106,12) H4(4,30),距板边 4mm,**不在四角**,最低的一颗在 y=79 —— 下方 66mm 板边(挂着 7 个被线拽的端子)没有支撑 | H4(4,30) H3(126,30) H2(4,79) H1(126,79),距板边 4mm。**另建议加第 5 颗 H5 (103,137)**,落在 J3 与 J1 之间的 9.6mm 空当里,专门顶住被线拽的下板边 |
| 8 | Type-C 与 XH/Qwiic/KF128 放上板边,插拔方向朝外 | ✅ 满足(J2/J9/J10/J11 都在 y≈3–4) | 保持:J2 在 A2 区上板边,J11/J10/J9 在 A3 区上板边 |

**关于第 4 条把 20mm 放宽到 12mm:**
20mm/面这个数在 `layout-guide.md` 里没有给推导。按铜箔电阻率算(1oz = 35µm,ρ=1.72×10⁻⁸ Ω·m):

- 12mm 宽单层:`0.0410 mΩ/mm`;脊椎长 91.5mm,其中 36mm 底层让给信号车道
- 总电阻 ≈ `36×0.0410 + 55.5×0.0410/2` = **2.61 mΩ**
- **15A → 39mV / 0.59W;12A(正常运行上限)→ 31mV / 0.38W**
- 载流(IPC-2221 外层,A = 12×0.035 = 0.42mm²):单层 **14.5A @ ΔT=10℃ / 19.7A @ ΔT=20℃**,双层再翻倍

也就是说 12mm 双面对 15A 有约 2 倍余量,压降是 24V 的 0.16%。
20mm 会更好,但会把脊椎带从 12mm 撑到 20mm,把通道列的竖直预算吃掉 8mm。
**我按 12mm 排,如果你要按原文的 20mm 走,通道列就要少一行 —— 请拍板。**

### A5.2 `netlist-spec.md` 关键电气/几何约束

| 块 | 约束 | v2 怎么满足 |
|---|---|---|
| A | Q1/Q2 P-MOS 高边防反接,DZ1 钳 Vgs ≤12V | D0 第 4 行,DZ1/R1 贴在 Q1/Q2 右侧空当,栅极环路 ≤10mm |
| A | **RS1 必须开尔文**,IN+/IN− 单独细线、并行等长 | 见 A5.1 第 3 条 |
| A | U1 地址脚 A0=A1=GND(0x40) | 网络约束,布局无关;U1 底下就是地基准点,两脚直接短过去 |
| A | **C1–C5 尽量靠近 J1** | ❌ 现版 83–101mm。v2 **37mm**,是"J1 在下板边 + F1/Q1Q2 串在中间"下的物理下限(§A3.2);另在 J1 跟前 ≤8mm 处新增 `V24_IN` RC 缓冲(§C1) |
| B | C32/C33/C34 **紧贴 U2 的 VIN 脚**;C38(Cboot)接 SW 不接地 | A4 区内 C32/C33 与 VIN 脚同侧、边到边 ≤2mm |
| B | RT/CLK(R62)走线最短、紧贴 IC;FB 走线**远离 SW** | R62 贴 U2 的 4 脚 ≤3mm;FB 分压 R63/R64 走 U2 背离 SW 的一侧,与 SW 铜净距 ≥3mm |
| B | U2 PowerPAD **必须焊到 GND 铜箔,正下方打多个过孔** | U2 焊盘正下方 ≥9 颗 0.3mm 过孔通到底层 GND |
| B | U2 开关环路(VIN→C32/C33→D2 阳极→GND)面积最小 | 见 A5.1 第 2 条 |
| B | 输入电容用 100V 档;C35 体电容必需 | 选型约束,布局上 C35 紧邻 C32/C33 |
| C | **CH340C 用 3.3V 供电**;CC1/CC2 各一颗 5.1k 下拉;**IO12 悬空** | 网络约束;布局上 R9/R10 贴 J2 的 CC 脚 ≤5mm |
| C | C10 紧贴 U4 的 3V3 脚 | A1 区内 C10/C11 放在 U4 的 3V3 引脚一侧 ≤3mm |
| C | 自动下载交叉接法(DTR→R11→Q4→EN;RTS→R12→Q5→IO0)不可对调 | 网络约束;Q4/Q5 与 U5 同区相邻 |
| D | **OE_CTRL 绝不可直连 OE_N**,必须经 Q6 | 网络约束;Q6/R13/R14/R15 全在 A5 区,与 U6/U7 的 /OE 脚同区 |
| D | C14/C15 紧贴 U6/U7 的 VCC | A5 区内各自贴 VCC 脚 ≤2mm |
| D | U7 未用输入 A5–A8 必须接 GND | 网络约束 |
| E | **续流 D5/D6 方向:阳极→漏极,阴极→CHn_VOUT**(接反即短路灯带) | 布局上把 D 续流行(y=112.6)夹在电解行与 TVS 行之间,阴极统一朝上(朝 `CHn_VOUT`)、阳极统一朝下(朝漏极),**六列十二颗方向一致**,肉眼一扫就能查 |
| E | 必须 SS36B(60V),不可 SS34 | 选型约束 |
| E | 每路 C16/C17 尽量靠近端子 J3 | **拆开看**:`C17`(100nF)从"贴端子"改成"贴 MOS/续流"(y=123.0),因为它真正的作用是闭合快速换流环路,现版把它放在 y=71.8、离 D5/Q7 竖直 49mm,等于没起作用;`C16`(100µF 电解)在 y=104.4、离端子 33.1mm(现版 59.5mm)。**这一条我是有意按"环路优先"重新解读的,写在这里备查** |
| E | **保险丝座远离 MOSFET 发热区** + 镊子空间 | 保险丝行 y=89.6,MOS 行 y=128.7,相距 **39.1mm**(现版 F2 y=126.3 / Q7 y=107.8,相距 18.6mm)。镊子空间见 A5.1 第 6 条 |
| F | 接口物理防呆:I2C 走 Qwiic(3.3V)、UART 走 XH(5V),丝印标电压 | A3 区三个接口在上板边并排,丝印 `I2C 3.3V` / `UART 5V` / `SW1-4 + GND` |
| F | GPIO34/35/36/39 无内部上拉,R58–R61 必需;**串阻在端子侧、上拉在 MCU 侧** | A3 区从 J11 往内依次是 R54–57(串阻)→ R58–61(上拉)+ C28–31,顺序即物理顺序 |

### A5.3 走线宽度(沿用 `layout-guide.md`,只改脊椎与漏极)

| 网络 | 现版 | v2 |
|---|---|---|
| 脊椎 `V24_IN → V24_BUS` | 覆铜 ≥20mm/面 ×2 面(实际是约 3.7mm 高的铜带) | **12mm × 双面 + 缝合过孔**,压降 39mV @15A |
| `CHn_VOUT` 载流段 | 2.0mm(10.7mm 长) | **目标 3.0mm,瓶颈处不低于 2.0mm**;长度增到 47.9mm(脊椎搬到了列顶) |
| `CHn_CW_D` / `CHn_WW_D` | 1.2mm(24.3 / 41.7mm 长) | **目标 2.0mm,不低于 1.2mm**;长度降到约 13mm,**两路等长** |
| `V24_LOGIC` / `SW_NODE` | ≥1mm | ≥1mm(`V24_LOGIC` 从 D0 上行到 A4 约 40mm,200mA 下压降约 7mV,估算) |
| `V5_SYS` / `V3P3` | ≥0.8mm | ≥0.8mm |
| 信号 | 0.25mm | 0.25mm |
| 开尔文采样对 | 0.3mm | 0.3mm,并行,长度差 ≤0.5mm |

---

## A6. 丝印预算

### 判据(直接沿用 `gen_silk_refdes_fix.py`,把它从"事后修补"变成"事前验收")

    设 ds = 位号文字到自己元件外框的距离
       do = 位号文字到最近的其它元件外框的距离
    要求 do ≥ ds + max(0.20mm, 0.5 × ds)

只要求 `do > ds` 是不够的:现版 F2–F5 那四个位号 **ds = do = 0.92mm**,正卡在两颗保险丝
中间,标的是哪一颗谁也说不清。必须"离自己近出一截"。

### 从判据反推出的空间预算

KiCad 默认位号文字 **高 1.0mm、线宽 0.15mm**;最长位号是 `LED10`–`LED13`(5 字符,约 5mm 宽)。
把文字贴在自己元件的上方或下方(ds = 0.3mm),要让隔壁行的元件满足 do ≥ 0.5mm:

    行间净距 ≥ ds + 文字高 + do = 0.3 + 1.0 + 0.5 = 1.8mm   ← 硬预算
    v2 设计值取 2.2mm,留 0.4mm 余量                          ← 设计值

**这就是 §A3.1 里"行间净距 2.20mm"这个数的来历。**

同一行里左右相邻的元件,位号**上下交错**(奇数个在上、偶数个在下),
或者水平净距 ≥ 文字宽 + 2×0.6mm。

### 各类封装的预算表

| 封装 | courtyard | 位号典型宽 | 摆位方向 | 所需净距 |
|---|---|---|---|---|
| 0603(R/C) | 1.69 × 0.89 | 3.0mm(`R59`) | 上或下 | 行间 ≥1.8,列间 ≥4.2 |
| 0805(LED) | 2.09 × 1.34 | 5.0mm(`LED13`) | 上或下 | 行间 ≥1.8,列间 ≥6.2 |
| SMB(SS36B / SMBJ26A) | 4.69 × 3.69 | 3.0mm | 上或下 | 行间 ≥1.8 |
| TO-252(20N06) | 6.69 × 6.14 | 2.5mm | 上或下 | 行间 ≥1.8 |
| Nano2 保险丝座 | 9.82 × 5.12 | 2.5mm | **下方**(上方是脊椎铜) | 行间 ≥1.8 |
| KF2EDGV 端子 | 12.31 × 7.09 | 2.5mm | 上方 | 行间 ≥1.8 |
| ELEC8 电解 | 8.40 × 8.39 | 3.5mm | 上或下 | 行间 ≥1.8 |

### v2 能不能把现版那 28 个"没有位号"的元件救回来

| 现版没位号的 | 数量 | 现版为什么排不下(实测) | v2 |
|---|---|---|---|
| LED2–LED13 | 12 | 灯与限流电阻中心距 2.1mm | ✅ **能标**。16mm 列宽下,电解占左半(8.4mm),右侧 7.4mm 里灯与电阻分两个子排,子排间净距 3.4mm > 1.8mm |
| R20/21/26/27/32/33/38/39/44/45/50/51 | 12 | 同上 | ✅ **能标**,同上 |
| R59 / R60 / R61 | 3 | R54–57 与 R58–61 两排的 y 间距 2.5mm,元件高 0.89 → **行净距 1.61mm < 1.8mm** | ✅ **能标**。A3 区把行距从 2.5mm 拉到 **3.5mm**(净距 2.61mm),列距从 4.0mm 拉到 **4.5mm** |
| TP6 | 1 | 被 LED2/LED3/R20/R21 四面围住,上下各只剩 0.35mm | ✅ **能标**。v2 里 TP6 从指示灯堆里搬到 CH1 列的栅阻行旁,四周 ≥2mm |

**目标:v2 摆位完成时,205 个元件里 0 个位号不达标、0 个位号被隐藏。**
验收方法就是把 `gen_silk_refdes_fix.py` 拿来当**检查器**跑一遍 —— 它一个都不用改动,
就说明摆位阶段的预算做对了。(现版跑它要重摆 52 个、隐藏 4 个。)

---

## A7. 与现版的差异清单

| # | 改什么 | 现版 | v2 | 为什么(追溯到原则) |
|---:|---|---|---|---|
| 1 | **板框** | 110 × 145 | **130 × 145** | 下板边放不下 6 个输出端子 + 1 个输入端子 + 合理列距(§A4)。**代价:亚克力上下夹板要重画重切** |
| 2 | **天线区禁铜** | ❌ GND 覆铜双面填满天线正下方,板上无任何 keepout 规则区(实测) | A0 区 x 0–8 / y 0–25 设双面 keepout | `layout-guide.md` 第 1 条;地平面直接压在 PCB 天线下会失谐、砍掉大半通信距离 |
| 3 | **入电保护链的走向** | 上行 38.3mm → 掉头下行 33mm → 左行 84mm | 上行 55mm → 左行 93mm → 每列下行 48mm,**无折返** | 原则 1 |
| 4 | **体电容 C1–C5 的位置** | (42..98, 53.75),离 J1 **83–101mm**,挂在一条 46mm 的死头支路末端 | D0 区 y 90.5 与 100,离 J1 **37mm**,串在主链上 | `netlist-spec.md:45` 明写"C1–C5 尽量靠近 J1",现版是**对自己规格的违反** |
| 5 | **分配脊椎** | 底层 y 128–132 的约 3.7mm 铜带 + 一段竖带,顶层几乎没有 | **双面 12mm × 91.5mm + 每 3mm 一颗缝合过孔**;底层每列让 6mm 信号车道 | `layout-guide.md` 第 4 条;顺带给强弱电交界定了唯一的穿越面(原则 2) |
| 6 | **U6 / U7 的位置** | U6 x=76 驱 CH1–4(x=80/66/52/38);**U7 x=54 驱 CH5/CH6(x=24/10)**,栅极信号横跨 44–57mm 并穿插进别的列 | U6 x=68(CH1–4 质心)、U7 x=20(CH5–6 质心);12 根信号垂直下行,**脊椎以下每列只走本列的网络** | 原则 3 |
| 7 | **MOSFET 排布** | 上下两排(y=95.94 / 107.77),两路漏极走线 24.3mm 与 41.7mm,**不对称** | 并排(同一行 y=128.7),漏极走线约 13mm,**两路等长** | 原则 1(缩短 3A 路径)+ 原则 6(保持六列同行) |
| 8 | **栅阻位置** | 100Ω 在驱动侧,栅极侧只有 10k 下拉;栅极到栅阻约 36mm 无阻尼 | 100Ω 与 10k **都在 MOS 栅极旁**(列内 y=123.0),无阻尼段 ≈ 3mm | 抗 dV/dt 误导通 + 阻尼走线电感与 Ciss 的谐振(§A3.1)。**只改摆位,原理图不动** |
| 9 | **列距** | 14mm | **16mm** | 让两只 TO-252 并排、让 24 颗指示灯与限流电阻排得下位号 |
| 10 | **保险丝镊子空间** | F2 上方净距 0.72mm、下方 0.99mm(实测),按字面不满足 8mm | 保险丝行下方 **8.00mm** 内无元件,上方 2.04mm 是光铜面 | `layout-guide.md` 第 6 条 |
| 11 | **保险丝与 MOS 的距离** | F2 y=126.3 ↔ Q7 y=107.8 = 18.6mm | 89.6 ↔ 128.7 = **39.1mm** | `netlist-spec.md` Block E"保险丝座远离 MOSFET 发热区" |
| 12 | **F1 与 Q1/Q2 的距离** | 净距 3.3mm | 净距 **10.8mm**,且 P-MOS 散热铜面朝背离 F1 的方向铺 | 设计文档 §7:输入 P-MOS 约 0.8W/只(高温),"另设独立散热铜面并远离保险丝座" |
| 13 | **安装孔** | (106,79)(4,65)(106,12)(4,30),全在上半部,下板边 66mm 无支撑 | (4,30)(126,30)(4,79)(126,79);**建议加第 5 颗 H5 (103,137)** 顶住被线拽的下板边 | `layout-guide.md` 第 7 条 + 机械 |
| 14 | **F7 的 0.9mm 偏移** | F7 x=10.9,其余五颗在 14mm 等距上;当年不能对齐是因为对齐后 CH6 输出焊盘会压上 TP2(短路) | 六颗保险丝**严格 16mm 等距**;TP2(GND)搬到 D0 区 RS1 旁边,不再挡路 | 原则 6(规整)+ 顺带解决一个历史遗留 |
| 15 | **丝印位号** | 28 个元件没有位号(排不下);修补脚本重摆 52 个、隐藏 4 个 | **目标 0 个不达标、0 个隐藏**;`gen_silk_refdes_fix.py` 当验收器跑,一个都不用改 | 原则 4 |
| 16 | **GND 单点** | 一整片覆铜,没有几何隔离 | 写成两条可检查规则(§A5.1 第 5 条):功率回流限定在 y ≥ 133 的底部带;U1 地脚独立细线接 RS1 旁地基准点 | `layout-guide.md` 第 5 条 |
| 17 | **`CHn_VOUT` 线宽** | 2.0mm(10.7mm 长,2.6mΩ) | **3.0mm**(47.9mm 长,7.9mΩ) | 脊椎搬到列顶后这一段变长了,用线宽补回来;配合漏极缩短,单路总串阻 12.6/19.7mΩ → **11.0mΩ 且两路一样**(§A2.1) |
| 18 | **24V 路径总长** | 174mm,含 33.3mm 折返 | 206mm,**无折返** | 长度是代价、方向是收益 —— 这一条我明说,不粉饰(§A2.1) |
| 19 | **`C17` 等 6 颗 100nF 的位置** | 跟着电解放在 y=71.8,离 D5(121.2)/ Q7(107.8)竖直 **49mm** | 放进栅阻行 y=123.0,夹在续流管(118.5)与 MOS(128.7)之间 | 它的作用是闭合 `C17 → D5 → Q7 → GND` 这个每周期换流的快速环路;环路面积 49mm 跨度 → **约 0.8 cm²** |

**不改的东西**(明确列出来,免得下一轮误伤):

- 原理图网络拓扑一律不动(§C 的新增件除外)
- "所有元件顶层"不变 —— 虽然已确认改走标准型 SMT(支持双面),单面焊接仍然更省、更好返修
- 六列等距、六列同行的骨架不变(原则 6)
- 板高 145mm 不变 —— 亚克力背板与装配顺序不用重算
- 接线端子全部在下板边不变 —— 这是人类在 v15 明确要求的
- `R2` 首版接 GND(Q3 恒关)不变 —— `netlist-spec.md:42` 说好"验证通过后再改接 GPIO",而这块板还没验证过

---

## C. 这一轮建议一起改的原理图改动(**只列,不改**)

### C1. `V24_IN` 上没有任何电容 —— 建议留一组 RC 缓冲位

**现状(实测)**:`V24_IN` 网只有 **2 个焊盘** —— J1 的进线脚和 F1 的一头。
从接线端子到防反 P-MOS 之间,一颗电容都没有。板上最靠前的电容是
`V24_PROT` 上的 C1–C5,而它们在 Q1/Q2 之后。

**建议**:在 `V24_IN` 与 `GND` 之间留 **一组串联的 RC 焊位**,离 J1 焊盘 ≤8mm、回路面积 ≤50mm²:

| 新位号 | 封装 | 首装建议值 | 说明 |
|---|---|---|---|
| `C44` | 1210 | **1µF / 100V X7R** | 100V 档的理由与 C32/C33 相同(`netlist-spec.md` Block B:42.1V 瞬态下 50V 档只有 1.19× 裕量,且 X7R 在 24V 偏置下衰减 40–50%) |
| `R68` | 1206 | **2.2Ω / 1W** | 阻尼电阻,与 C44 串联 |

> ⚠️ **这两个值是"起始值",不是算出来的。** 缓冲电阻的正解是 `R ≈ √(L/C)`,
> 而 L 是**你那根 24V 线缆的电感**,现在没有实测数据。我不会假装能算准。

**上电后怎么测、怎么定值:**

1. **先测本底**:`R68` 位置贴 0Ω、`C44` 空贴。示波器(10:1 探头,**用地弹簧,不要鳄鱼夹**)
   探在 J1 的焊盘上,按最恶劣条件热插拔 24V(长线缆 + 满载),记录振铃频率 `f0` 与峰值 `Vpk`。
2. **再测加了已知电容之后**:装上 `C44` = 1µF,重测频率 `f0'`。
3. **解出线缆电感**:由 `ω0² = 1/(L·Cs)` 与 `ω0'² = 1/(L·(Cs+C44))` 两式解出 `L` 与杂散电容 `Cs`。
4. **取阻尼电阻**:`R68 ≈ √(L/(Cs+C44))`(临界阻尼量级),就近取标称值。
   功耗按脉冲能量 `½·L·I²` 算,重复率就是插拔次数,稳态几乎为零 —— 1W 档足够。
5. **复测验收**:目标是让上电振铃峰值**低于 D1(SMBJ26A)的击穿门槛**,
   使 TVS 只在异常时导通、不要每次上电都工作(具体门槛以 SMBJ26A 手册的 V_BR 最小值为准)。
6. **如果实测根本不需要**:`R68` 贴 0Ω 或两个位都空贴,留着不影响任何东西。

**成本**:两个焊位,BOM 上两行,首装约 ¥0.3。**留位的成本远低于回板后发现要飞线的成本。**

### C2. `RS1` 停产 —— 建议换料

| 项 | 现用 | 建议替代 | 备选 |
|---|---|---|---|
| 型号 | LR2512D-2W-2mR-1% | **HoLR2512-3W-2mR-1%** | HoLLR2512-2W-2mR-1% |
| 编号 | `C500614`(页面标停产) | **`C2844506`** | `C2962780` |
| 规格 | 2512 / 2mΩ / ±1% / 2W | 2512 / 2mΩ / ±1% / **3W** / TCR ±75ppm/℃ | 2512 / 2mΩ / ±1% / 2W |
| 库存 | — | 3,210(查询时) | 1,850(查询时) |
| 嘉立创贴装 | — | ✅ 元件页写明支持 SMT 贴装(经济型与标准型) | 未逐项核实 |

**推荐 `C2844506`(3W 档)**:封装、阻值、精度全部对得上,不用改封装,功率档从 2W 升到 3W。
校核:`P = I²R`,12A 时 0.29W、15A 时 0.45W,3W 档有 6.7 倍余量。TCR ±75ppm/℃ 有明确标定,
对 INA237 的测量精度是加分项。**下单前请在嘉立创 SMT 元件库再确认一次实时库存与价格。**

### C3. `V24_PROT` / `V24_BUS` 上一颗陶瓷都没有 —— 建议加两颗

**现状**:24V 母线上的储能全是铝电解(C1–C5)。12 只 MOSFET 在 19.5kHz 下同时开关的
高频电流,只能通过电解电容的 ESR 供给。六路的 100nF(C17/C19/…)挂在**保险丝下游**的
`CHn_VOUT` 上,帮不到母线本身。

**建议**:新增 **`C45` / `C46`,1µF / 100V X7R 1210**,分别放在:
- `C45`:D0 区分配点旁(RS1 下游,`V24_BUS`)
- `C46`:脊椎最远端(CH6 保险丝上游,`V24_BUS`)

这是**建议**不是必需项 —— 我没有实测母线纹波,也没有算 ESR 的具体贡献。
成本两颗 1210,焊位在脊椎带边缘现成有地方。若要省,先装 `C45` 一颗。

### C4. 测试点建议加两个

| 新位号 | 网络 | 为什么 |
|---|---|---|
| `TP7` | `PMOS_GATE` | 首板上电时验证防反接 P-MOS 真的导通了(Vgs 应约 −10~−12V,被 DZ1 钳住)。现在这个节点没有任何探测点,而它一旦没导通,整板不通电,排查时会先怀疑保险丝 |
| `TP8` | `GND`(放在 A4 区 buck 旁) | 现有的 `TP2`(GND)在 D0 区。测 buck 的 SW/FB 波形时需要就近的地参考,拉长地线会把开关噪声全测进来 |

### C5. 顺带核一遍供货(不改设计,只是复查)

- **`R63` = 102kΩ ±1% 0603 `C402870`** —— `netlist-spec.md:89` 自己就标了"松下,厚声缺货"。
  下单前复查一次;这是 buck 的 FB 上分压,阻值不能随便代。
- **`L1` `C9400`(33µH / Isat ≥5.5A / 12.3×12.3mm)** —— 单一料号、体积大,值得提前确认库存。
- **`U1` INA237AIDGSR `C2864837`** —— 未见停产标记,顺手复查。

### C6. 我读规格书时发现的、但**建议不改**的两处

- **栅极驱动能力**:5V 驱动 + 100Ω 栅阻下的边沿速度与开关损耗,设计文档 §6.3 已经
  明确列为"**必须实测**"项(§11 第 3 条),TP5/TP6 就是为它留的。v2 保留 TP5/TP6
  并让栅极路径变得可测(栅阻紧贴栅极,TP5 就在旁边)。**这一轮不动,等首板波形。**
- **指示灯接在功率回路上**:`netlist-spec.md` Block E 已记录了这条的代价与新增失效模式,
  并注明"2026-08-13 经人类拍板接受"。**不重开。**

---

## 需要人类拍板的四件事

1. **板框到底多大?** 我推荐 **130×145**。若你更看重亚克力免费打样窗口,可以按 110×145 走,
   降级清单在 §A4 末尾,是真的有代价的。
2. **亚克力夹板现在切还是等?** 采纳 130mm 之后,现有的 `driver-top.svg` / `driver-bottom.svg`
   会作废(四个孔里三个要挪)。这与"8/31 免费打样截止,建议现在就切"直接冲突。
3. **脊椎按 12mm 还是按 `layout-guide.md` 原文的 20mm?** 12mm 双面对 15A 有约 2 倍余量、
   压降 39mV;要 20mm 的话,通道列要少一行。
4. **要不要加第 5 颗安装孔 H5?** 板子下边缘挂着 7 个被线拽的端子,现版最近的支撑在 66mm 之外。
   加了 H5 亚克力上就是 5 个孔、5 根铜柱。

---

## 附:本文件里的数是怎么来的

- **封装尺寸**:`pcbnew.LoadBoard()` + `GetCourtyard(F_CrtYd).BBox()`,逐个从 `cct-main.kicad_pcb` 取
- **现版元件坐标**:同上,`GetPosition()`
- **覆铜范围与天线区禁铜**:`GetArea(i).GetBoundingBox()` 与 `HitTestFilledArea()` 逐点验
- **网络焊盘数**:遍历所有 footprint 的 pad,按 `GetNetname()` 计数
- **元件总数 205 与归区清点**:`gen_floorplan_svg.py`,位号读 `cpl-jlc.csv` + 10 个非贴装件
- **压降 / 载流**:铜箔电阻率 ρ=1.72×10⁻⁸ Ω·m、1oz = 35µm;载流按 IPC-2221 外层公式
  `I = 0.048 · ΔT^0.44 · A^0.725`(A 单位 mil²)。**这两项是估算,已注明**
- **替代料号**:立创商城 / 嘉立创元件页,查询日期 2026-08-14

参考:
[HoLR2512-3W-2mR-1% (C2844506) — LCSC](https://www.lcsc.com/product-detail/C2844506.html) ·
[C2844506 — 嘉立创元件页(支持 SMT 贴装)](https://jlcpcb.com/partdetail/C2844506) ·
[HoLLR2512-2W-2mR-1% (C2962780) — LCSC](https://www.lcsc.com/product-detail/Chip-Resistor-Surface-Mount_Milliohm-HoLLR2512-2W-2mR-1_C2962780.html)
